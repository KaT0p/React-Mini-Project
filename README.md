# React Mini Project

```bash
    # ติดตั้ง Package 
    # ในโฟลเดอร์ Client และ Service
    $ npm install

    # Run Vite ด้วย Command
    $ npm run dev

    # สามารถ Start ตัว Server ด้วย Command 
    $ npm start 

```